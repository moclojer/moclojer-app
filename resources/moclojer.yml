  - endpoint:
      method: GET
      path: /version
      response:
        status: 200
        headers: {Content-Type: application/json}
        body: |-
          {
            "version": "v0.0.1!"
          }
